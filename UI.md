# UI について

このアプリケーションの UI は、ratatui クレートを利用した TUI となっている。
まず、上から順番に、

- top_bar (height: 1)
- middle_box
- bottom_bar (height: 1)

をならべ、そして、middle_box の中に、左から順番に

- left_panel (min_width: 1) // 左側にタブバーを用意しておき、上から下へタブを並べられるようにする。タブの最小幅が 1 となる。
- center_box
- right_panel // 上側にタブバーを用意しておき、左から右へタブを並べられるようにする。タブの高さは 1 となる。

を並べ、そして、center_box の中に、

- main_panel // 主にエディターなどを設置する予定。　上側にタブバーを用意しておき、左から右へタブを並べられるようにする。タブの高さが 1 となる。
- sub_panel // 主にターミナルマネージャーなどを設置する予定。上側にタブバーを用意しておき、左から右へタブを並べられるようにする。タブの高さが 1 となる。

を並べるようにする。

また、それらとは独立して、画面の右下側に、下から上に積み上がり、下から順番に表示が消えるような通知機能を実装する。
そして、上側中央に、上側に少し隙間を開けて、コマンドパレットを設置しておき、キーバインドなどでフォーカスが当たっている間のみに表示し、 ">main.system.settings.open"などを入力することで特定のコマンドを実行し、"src/main.rs"などで適宜ファイルを検索しながらファイルを開いたりできるようにする。これらのパーサーについては、今後改造を加えられるようにしておくこと。

# event について

event(キーボード入力)の処理順は次のようにする

1. システム(例えば、それぞれのパネルを隠したり表示したり、それぞれのパネルにフォーカスを当てるためのキーがこれに該当する)

2. フォーカスの当てられているパネル

3. フォーカスが当てられていないパネル

4. その他

どこかでキーバインドが処理された場合、残りのものにはキーを送信しないようにする。

# config について

config は、"main.main_panel.editor.keybind.copy"などのように、id と、
ConfigValue::Keybind, ConfigValue::Int などの enum で config を指定できるようにすること。
設定は yaml で
`~/.config/console-ide/configs/main.yaml`

に保存するようにすること。

また、それとは別で、カラーテーマについても、設定をできるようにしておくこと。

# features について

features はこの console-ide において最も重要な部分である。
これらは、エディターや、ターミナルエミュレータ、ファイルビュワー、Git 管理機能や AI とのチャット機能など、それぞれに応じて様々な機能を処理できるようにする。
features には、エディター、ターミナルエミュレーター、プロブレムビュワーなどの、build_in 機能と、このクレートを利用して別途 Rust 言語で開発して、コンパイルしたバイナリを通じてプロセス間通信を利用して機能する extend の二種類を用意しておくこと。

それに準じて、

- 特定のターミナルの表示をウィジェットとして登録する機能
- ボトムバーに状態などを表示できるようにする
- トップバーに特定の操作などを id で登録できるようにする
- コマンドパレット経由で実行できるコマンドを登録できるようにする
- イベントハンドラー
- イベントセンダー
- 設定

などを自由にできるようにしておくこと

# その他

その他、なにか疑問に思ったり、迷ったりした点があれば、私に聞いてください。
